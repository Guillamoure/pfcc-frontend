

export const initialCharacterDistribution = (character) => {
  // racial traits
  // klass features
  // weapon
  // armor
  // magic items
}

const klassFeaturesFeatureDistribution = (kfs) => {

}
